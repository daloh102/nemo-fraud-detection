scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Füge diesen Block hinzu:
  - job_name: 'gpu-metrics'
    static_configs:
      - targets: ['localhost:9400']